.env
.env.*
!.env.example

# Local design archive (contents committed under design/)
nyc-events-frontend-prototype.zip

# Skill-suite source: tooling, not project content.
# Installed to ~/.claude/skills — excluded so it stays out of the graphify graph.
claude-code-issue-worker-skill-suite/

# graphify: machine-local hook paths (re-create with `graphify claude install`)
.claude/settings.local.json

# graphify: local scan cache (mtime/seen timestamps), dated backups
graphify-out/cache/
graphify-out/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]/
graphify-out/manifest.json
