## graphify

graphify is available locally as an optional code-navigation aid (`graphify query`, `graphify explain`, `graphify path`). Outputs under graphify-out/ are machine-local and untracked — regenerate with `graphify update .` when useful. There is no CI gate and no freshness requirement.
